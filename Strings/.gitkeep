// Problem: Reverse String
// Topic: Strings
// Difficulty: Easy

#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main() {
    string str = "hello";

    reverse(str.begin(), str.end());

    cout << "Reversed String: " << str << endl;

    return 0;
}
